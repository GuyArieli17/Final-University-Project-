#!/usr/bin/env python

from setuptools import setup

# This setup is suitable for "python setup.py develop".

setup(name='simulation',
      version='0.1',
      description=' this is an abstraction that implement gym.env api',
      author='GuyArieli17',
      author_email='private',
      url='http://www.google.com',
      packages=['simulation'],
      )